name: Check Activation
on:
  workflow_dispatch: {}
jobs:
  activation:
    name: Check activation file
    runs-on: ubuntu-latest
    steps:
      # Request manual activation file
      - name: Unity - Activate
        uses: game-ci/unity-activate@v1.3

